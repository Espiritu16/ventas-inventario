# app/Compartido

Código usado por más de un dominio: tipos de valor, reglas de validación y
utilidades transversales. Namespace `App\Compartido`.

Lo que pertenece a un solo dominio no va aquí, va en `app/Dominios/<Dominio>/`.
