---
id: S-02-B
name: Catálogo — categorías y productos
role: implementation-backend
repository: ventas-inventario
status: EN_VALIDACION
branch: sprint/S-02-B
base_sha: b1c7b133d3762985f81c50b95051396ba785e456
final_sha: 9c5c605105eade23f5b4e5ff250fa740f7efebc1
worktree_path: /private/tmp/claude-501/-Users-sankef-ventas-inventario/8fefec88-810c-4dd5-b0d7-6da99cf44f83/scratchpad/S-02-B
gobierna: develop@fc0ddbb
updated_at: 2026-08-20
---

## Objetivo

Categorías y productos con sus dos precios, unidad de medida válida para el
comprobante y stock mínimo. Sin existencias: el stock lo crea la compra en
S-04-B.

## Implementado

| Unidad | Qué quedó | Estado |
|---|---|---|
| UT-01 | Tablas `categorias` y `productos` (MIG-002), con la regla de precios, la positividad y el stock mínimo no negativo sostenidos por la base | verificado |
| UT-02 | Alta, listado y edición de categorías, con unicidad de nombre que no distingue mayúsculas | verificado |
| UT-03 | Alta, listado, consulta y edición de productos, con búsqueda por código y nombre y código inmutable | verificado |
| UT-04 | Todo cambio de precio queda en la bitácora con su valor anterior y su responsable | verificado |

### Decisiones que conviene conocer al revisar

- **Los precios se manejan como cadena decimal de punta a punta** y se comparan con `bccomp`. Convertirlos a punto flotante para validarlos perdería exactitud justo donde se decide si un precio es válido.
- **La validación numérica rechaza en vez de interpretar**: separador de miles, coma decimal, notación científica, signo, espacios y decimales de más. Aceptar `1,000.50` como mil o como uno cambia el precio de venta según quién lo lea (RNF-010).
- **La regla de precios se comprueba combinando lo enviado con lo actual**: cambiar solo uno de los dos también puede romperla.
- **La auditoría registra una lista explícita de campos**, no "todo lo que cambió", para que un campo nuevo no empiece a registrarse solo (RNF-014). Guardar el mismo precio no deja rastro: la bitácora no se llena de ruido.
- **La unicidad de nombre y de código se comprueba aparte de la validación de formato**, para que el rechazo lleve su propio código: un nombre repetido y uno demasiado corto son problemas distintos.

## Corrección transversal incluida — la URL pedida sin sesión

Pedida por Coordinación durante este sprint, en commit propio (9c5c605). Es
consecuencia de haber declarado las rutas de usuarios como pantallas: el
middleware seguía tratando toda petición como si fuera de API.

Un anónimo que pedía una pantalla recibía un 401 de texto plano —sin destino,
sin forma de identificarse y sin memoria de lo que pedía—, incumpliendo la
regla transversal de `docs/frontend/experiencia.md`. Ahora va a `/login` con la
URL pedida guardada, incluida su consulta, y vuelve ahí tras identificarse. Las
peticiones JSON y las de Livewire conservan el 401 de la taxonomía: un
componente no sabría qué hacer con una redirección en respuesta a una
interacción.

Dos detalles que quedaron escritos en el código porque el camino natural falla:

- **`redirect()->guest('/login')` no sirve en este middleware.** Solo guarda la intención si el enrutador ya resolvió la ruta, y este middleware es global, así que corre antes: con `guest()` la persona volvía siempre a la raíz en lugar de a lo que había pedido — el mismo defecto con otra cara.
- **`$peticion->session()` lanza «Session store not set on request»** en un middleware global. Hay que usar el almacén del contenedor, que sí está disponible.

## Mutación del mecanismo — práctica adoptada

Se rompió a mano cada mecanismo que este sprint dice garantizar y se verificó
que la suite lo detecta. Árbol restaurado después de cada una.

| Mutación | Resultado |
|---|---|
| Quitar el CHECK `productos_precio_mayor_valido` de la migración | falla 1 prueba |
| Hacer que la guarda del código inmutable nunca se dispare | fallan 2 pruebas |
| No registrar el cambio de precio en la bitácora | fallan 2 pruebas |
| No guardar `url.intended` al redirigir | fallan 3 pruebas |
| Devolver 401 también a las peticiones de pantalla | fallan 6 pruebas |

Las dos últimas se repitieron sobre código ya commiteado, después de un error
propio: la primera vez se mutó con la corrección aún sin commitear, y el
`git checkout` de la restauración se llevó el arreglo. El commit quedó con
pruebas nuevas y código viejo, y la suite en rojo. Se detectó al correr la suite
completa después del commit, se rehízo el cambio y se enmendó el commit, así que
en el historial no queda ningún estado roto. **Mutar solo sobre árbol limpio**:
si hay trabajo sin commitear, restaurar se lo lleva y uno queda creyendo que
verificó algo.

## Evidencia de verificación

| Comando | Resultado |
|---|---|
| `php artisan test --testsuite=Feature` | passed — 168 pruebas, 326 aserciones |
| `php artisan test --testsuite=Unit` | passed — 4 pruebas, 4 aserciones |
| `./vendor/bin/pint --test` | passed |
| `pnpm build` | assets compilados |
| `php artisan migrate` | MIG-002 aplica desde base limpia |
| Servidor real (8000): `/up` | 200 |
| Servidor real: `/productos` y `/categorias` sin sesión | 401 |
| Servidor real: `/usuarios` | 404 — retirada, ver abajo |

Base del carril: `ventas_inventario_s02b_test`, seleccionada por variable de
entorno. `phpunit.xml` conserva el nombre del proyecto: el del carril llegó a
commitearse por error y se corrigió, porque fusionarlo habría cambiado la base
de pruebas del proyecto entero por la de un sprint.

## Propuesta pendiente de Arquitectura — unidades de medida

### 1. Qué publica realmente la fuente

El Catálogo 03 de SUNAT **no enumera códigos**: delega en **UN/ECE
Recommendation 20 Rev 13**, del orden de mil ochocientos códigos. La lista "de
SUNAT" que se busca no existe como tal.

**No pude contrastar contra Rec 20.** El archivo que enlaza el anexo
(`unece.org/.../rec20_Rev13e_2017.xls`) devuelve **403**; el listado HTML de
Peppol no respondió; los anexos PDF de SUNAT no son legibles con las
herramientas de esta sesión. Lo que sigue **no está verificado contra la
fuente**, y conviene que lo contraste quien sí pueda abrirla.

### 2. Subconjunto que propongo adoptar

Los 62 códigos de `config/sunat.php`, que provienen de una guía de referencia
para Perú. El criterio de recorte —de mil ochocientos a 62— es que la
validación existe para que un error de carga no llegue al comprobante: aceptar
el catálogo completo la volvería inútil, porque casi cualquier cadena de tres
letras sería válida.

Quedan dentro: unidades y conteo (incluida `NIU`, el default, y `ZZ` para
servicios), longitud, superficie, volumen, peso, empaques y presentaciones, y
energía.

Quedan fuera, con su motivo: todo lo que una comercializadora no factura
—unidades científicas, eléctricas, de presión, de tiempo, de datos y de
frecuencia—. Ninguna aparece en RF-004 ni en el modelo de negocio; agregar una
después es una línea.

### 3. Un punto que conviene verificar antes de aprobar

Al buscar `NIU` en Rec 20, las referencias que encontré lo describen como
*number of international units* —una unidad de actividad biológica—, no como
"unidad de bien", que es el uso peruano y el valor por defecto que fijamos. **No
lo pude confirmar** y puede ser un artefacto de la búsqueda, pero si el uso
local difiere del significado del estándar, conviene que quede escrito en
`docs/integraciones/sunat.md` antes de emitir en S-06-B.

## Resultado QA

Pendiente — lo registra el Coordinador.

## Pendientes o desviaciones

1. **`stockDisponible` no se devuelve**, conforme al contrato enmendado: no hay lotes de los que calcularlo hasta S-04-B. Una prueba fija que el campo esté ausente, para que nadie lo agregue en cero sin darse cuenta.
2. **La proyección acotada del vendedor no tiene efecto todavía.** El contrato dice que el vendedor no recibe costo ni margen; hoy el producto no tiene ninguno de los dos —el costo nace con el lote en S-04-B—, así que no hay nada que ocultar. Cuando esos campos existan, el filtro por rol es trabajo de ese sprint y conviene que su RFC lo diga.
3. **`GET /` todavía no redirige.** La matriz declara que la raíz redirige a `/login` sin sesión y a `/panel` con ella, pero sigue devolviendo 200 y renderizando el layout, y las pruebas de S-00 lo fijan así. No se corrigió en este sprint: la ruta vive en `routes/web.php`, que no es de este rol, y `tests/Feature/Fundacion/` no está asignado a nadie en `AGENTS.md`. Separado por Coordinación, a la espera de una enmienda de gobernanza. No rompe nada; contradice la matriz.
4. **Corrección de S-01-B incluida en esta rama**: se retiraron los endpoints JSON de gestión de usuarios y se declararon `GET /login` y `GET /panel`. Va en un commit propio (77fed0f) y no es trabajo de S-02-B; viajó acá para no dejar parado al frente de interfaz.
