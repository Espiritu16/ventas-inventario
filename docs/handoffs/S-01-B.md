---
id: S-01-B
name: Acceso, usuarios y control de permisos
role: implementation-backend
repository: ventas-inventario
status: EN_VALIDACION
branch: sprint/S-01-B
base_sha: b99b93667d47bf49b18f8febabc5b0541f2df579
final_sha: 4156f116103cdf843bfeef84ab72798ca012760b
worktree_path: /private/tmp/claude-501/-Users-sankef-ventas-inventario/8fefec88-810c-4dd5-b0d7-6da99cf44f83/scratchpad/S-01-B
gobierna: develop@59a8a02
updated_at: 2026-08-19
---

## Objetivo

Autenticación con sesión, alta y edición de usuarios, y control de acceso por
rol aplicado en el servidor sobre cada ruta existente y futura; más la
instalación de Livewire, que es la plataforma sobre la que S-01-F construye.
Sin pantallas: son de S-01-F.

## Implementado

| Unidad | Qué quedó | Estado |
|---|---|---|
| UT-01 | Tabla `usuarios` (MIG-001) con el rol restringido por CHECK, y el modelo con la contraseña guardada solo como hash y oculta en toda serialización | verificado |
| UT-02 | `POST /login` y `POST /logout`, con regeneración de sesión al autenticar | verificado |
| UT-03 | `GET/POST /usuarios` y `PATCH /usuarios/{id}`, según el contrato v1 | verificado |
| UT-04 | Deny-by-default como middleware global, con la matriz de permisos transcrita en tres listas trazables a sus secciones del documento | verificado |
| UT-05 | Tablas `auditorias` y `log_errores` (MIG-009), con UPDATE y DELETE revocados sobre auditoría | verificado |
| UT-06 | Livewire 4.4.1 operativo, con descubrimiento de componentes dentro de cada dominio y un componente de humo que renderiza e interactúa | verificado |

### Decisiones que conviene conocer al revisar

- **La validación vive en el servicio, no en el controlador.** Los componentes Livewire invocan los servicios directamente, sin pasar por una ruta: si validara la capa HTTP, la interfaz quedaría sin validar.
- **La normalización ocurre antes de validar.** Un correo con espacios se recorta, que es lo que el contrato promete, en vez de rechazarse por formato.
- **El login siempre calcula un hash**, exista o no el usuario, para que el tiempo de respuesta no distinga un correo inexistente de una contraseña equivocada. Los tres casos —inexistente, inactivo y contraseña incorrecta— devuelven el mismo código y el mismo mensaje.
- **El control de acceso es middleware global, no del grupo `web`.** Ver "Hallazgos" — es el punto más importante de este sprint.
- **Las rutas de Livewire se declaran relativas a su prefijo**, resuelto en cada petición desde `EndpointResolver`. El paquete lo deriva de `APP_KEY`, así que difiere entre instalaciones.
- **Los componentes Livewire se descubren automáticamente** bajo `app/Dominios/*/Livewire/`. Si hubiera que declararlos a mano en el service provider, agregar una pantalla obligaría al frente de interfaz a tocar un archivo del backend.

## Hallazgos

### El control de acceso no alcanzaba a todas las rutas

Con el middleware en el grupo `web`, `GET /up`, los assets de Livewire y
`GET/PUT /storage/{path}` quedaban fuera: ese grupo solo alcanza a las rutas
que lo declaran, y ninguna de ellas lo declara.

Lo grave no es que `/up` respondiera —debe responder—, sino **por qué**
respondía: por no estar alcanzado por el control, no por estar declarado. Y
`/storage/{path}`, que la gobernanza declara **no autorizada**, quedaba
accesible. Dos pruebas mías pasaban por la razón equivocada: la de `/up`
porque el resultado coincidía por accidente, y la de `/storage` porque el
framework devolvía 403 por su cuenta al no existir el archivo.

Corregido registrando el control como middleware global, que resuelve la ruta
por sí mismo cuando el enrutador todavía no lo hizo. Ahora `/storage/x.txt`
responde 401 sin sesión y 403 con sesión, ambos con el código de la taxonomía.

### El prefijo de Livewire se deriva de `APP_KEY`

No es aleatorio por instalación: sale de
`substr(hash('sha256', app.key.'livewire-endpoint'), 0, 8)`. Es distinto en
cada máquina, en CI y en el servidor, así que una declaración con la cadena
literal habría sido correcta solo donde se generó y habría dejado de aplicar en
silencio en todas las demás. Se declara relativo al prefijo y se resuelve desde
la misma fuente que registra las rutas. Escalado y resuelto en la gobernanza.

### Corrección a un dato que reporté

Advertí que una lista enumerada de assets rompería producción por omitir
`livewire.min.js`. Verificado después: **en Livewire 4.4.1 no existe una ruta
`livewire.min.js`**; las rutas reales son `livewire.js` y dos `.map`. La
conclusión —declarar por patrón— sigue siendo la correcta, porque los mapas sí
existen y el conjunto puede cambiar entre versiones, pero el ejemplo concreto
que di era incorrecto y la gobernanza lo cita.

### Corrección de los hallazgos de QA (SHA 4156f11)

- **QA-01, un administrador podía desactivarse a sí mismo.** La regla `boolean` admite `false`, `0` y `"0"`, y `validated()` los devuelve tal como llegaron: la guarda comparaba contra `false` estricto y solo bloqueaba una de las tres. `"0"` es lo que envía un checkbox, o sea el camino normal desde la pantalla que S-01-F construirá. Consumado, el sistema quedaba sin nadie capaz de autenticarse como administrador y sin forma de recuperarse desde la aplicación.
  - Corregido casteando a booleano **después** de validar. Antes de validar habría ampliado lo aceptado y convertido entradas inválidas en válidas en silencio, contra RNF-010: `"false"` se sigue rechazando por tipo.
  - Cubiertas las tres formas admitidas, en el servicio y por HTTP, más el caso de `"false"` y el de desactivar a otro usuario, que sí debe funcionar.
- **QA-02, el login distinguía por tiempo un correo inexistente.** La mitigación creaba la señal que quería borrar: sin usuario se generaba el señuelo con `Hash::make` y se ejecutaban dos bcrypt contra uno del camino normal. Medido antes: 2.05x, unos 200 ms de diferencia. Ahora el señuelo es constante y precalculado, ambos caminos ejecutan exactamente una comparación, y la razón bajó a **1.03x** (211.8 / 205.8 / 207.9 ms sobre 12 intentos por caso).
  - La prueba cuenta las comparaciones de hash en vez de medir el reloj: la causa era estructural, y contar no depende de la carga de la máquina ni del coste configurado.
- **QA-03**, declarada la entrada del componente de inicio de sesión. Se declara por su clase, y el nombre con que se invoca se deriva de la misma fuente que lo registra: escribir la cadena a mano habría dejado que un renombre apagara el permiso en silencio.
- **QA-04**, corregido el motivo escrito en el comentario del patrón de assets.
- **Huecos de cobertura**: `preview-file` ya tiene prueba de comportamiento, y tanto ella como `upload-file` comprueban el código de la taxonomía y no solo el status.
- No se tomó SEG-02, según indicación del Coordinador.

## Evidencia de verificación

| Comando | Resultado |
|---|---|
| `php artisan test --testsuite=Feature` | passed — 89 pruebas, 184 aserciones |
| `php artisan test --testsuite=Unit` | passed — 4 pruebas, 4 aserciones |
| `./vendor/bin/pint --test` | passed |
| `pnpm build` | assets compilados |
| `php artisan migrate` | MIG-001 y MIG-009 aplican desde base limpia |
| Servidor real: `/`, `/up` | 200 |
| Servidor real: `/usuarios` y `/storage/x.txt` sin sesión | 401 con `NO_AUTENTICADO` |
| Desactivarse a sí mismo con `false`, `0` y `"0"` | rechazado con `NO_AUTORIZADO`; con `"false"`, con `CAMPO_FORMATO_INVALIDO` |
| Tiempos de login (12 intentos por caso) | 211.8 / 205.8 / 207.9 ms — razón 1.03x |

Cobertura de las tres condiciones pedidas:

1. **El control alcanza a los dos archivos de rutas**: `AlcanceDelControlTest` lo comprueba por mecanismo (el middleware es global) y por comportamiento en rutas de los cuatro orígenes: `routes/backend.php`, `routes/web.php`, el framework y el paquete.
2. **Rutas de Livewire**: assets públicos por patrón, `POST .../update` exigiendo sesión, y `upload-file`, `preview-file` y `storage/{path}` rechazándose. Una prueba comprueba además que el patrón de assets **no** cubre otros métodos ni otras rutas bajo el prefijo.
3. **`GET /` y `GET /up` responden por estar declaradas**: ambas pasan por el control, que las reconoce en la lista de infraestructura.

## Resultado QA

Pendiente — lo registra el Coordinador.

## Pendientes o desviaciones

1. **Resuelto**: la entrada del componente de inicio de sesión ya está declarada (`App\Dominios\Usuarios\Livewire\InicioDeSesion`, fijada en develop@59a8a02). S-01-F puede construirlo sin tocar rutas del backend. Si necesitara **otro** componente accesible sin sesión, sigue siendo decisión de Arquitectura y el archivo sigue siendo del backend.
2. **El componente de humo es temporal.** S-01-F puede borrarlo sin consultar: no implementa ningún RF.
3. **La auditoría de operaciones todavía no se escribe.** UT-05 crea las tablas y sus privilegios, que es su alcance; poblar `auditorias` en cada operación sensible es de S-08-B, según la matriz de cobertura.
4. **`pint.json` excluye `bootstrap/cache`**: Pint reformateaba archivos generados que no se versionan, y eso hacía fallar `pint --test` sin motivo.
