## RFC — S-01-B · Acceso, usuarios y control de permisos
- RF que implementa: RF-001, RF-002
- Rol: `implementation-backend`
- Alcance de este sprint: autenticación con sesión, alta y edición de usuarios, y control de acceso por rol aplicado **en el servidor** sobre cada ruta existente y futura; más la instalación de Livewire, que es la plataforma sobre la que S-01-F construye y que ningún sprint del horizonte declaraba (UT-06). **Explícitamente no**: pantallas (son de S-01-F), ni recuperación de contraseña, que ningún RF pide.
- Documentos relacionados: RNF-010 a RNF-014, RNF-004, docs/requisitos/actores-permisos.md, MIG-001, MIG-009, ADR-0005, docs/frontend/integracion.md
- Contratos que toca: docs/contratos/usuarios.md (v1)
- Estado: aprobado
- Aprobado por: Kevin Espíritu (kevinespiritu16@gmail.com) — fecha: 2026-08-19
- Enmienda 2026-08-19: se agregó UT-06, instalación de Livewire. Motivo: ninguno de los 19 RFC del horizonte instalaba la dependencia, pese a que `AGENTS.md`, ADR-0005, `docs/contratos/servicios-de-dominio.md` y `docs/frontend/integracion.md` la dan por existente. Lo detectó el chat de Frontend antes de su despacho. Se ubicó en este sprint y no en S-00 porque S-00 ya estaba fijando su `final_sha`; sin esto, S-01-F se bloquearía su primer día sin poder desbloquearse por sí mismo, porque `composer.json` no es ruta de `implementation-frontend`. Aprobada por: Kevin Espíritu (kevinespiritu16@gmail.com)

## Unidades de trabajo

> **Cómo leer la columna "Interfaz fijada"**: las rutas que aparecen ahí no son endpoints HTTP. Identifican el recurso y la operación. Ver [README de RFCs](README.md) y [ADR-0006](../decisiones/0006-sin-api-http-interna.md).

| ID | Resultado observable | Deriva de | Depende de | Paralelizable con | Interfaz fijada | Criterio de cierre |
|---|---|---|---|---|---|---|
| UT-01 | Fundación compartida: entidad `Usuario` con su migración, contraseña almacenada solo como hash y rol restringido por la base de datos | RF-001, RF-002, MIG-001 | ninguna | ninguna | schema de `usuarios` según el modelo aprobado | la migración aplica desde base limpia; un rol fuera de los dos permitidos es rechazado por la base, no solo por el código |
| UT-02 | Inicio y cierre de sesión | RF-001 | UT-01 | UT-03 | `POST /login`, `POST /logout` (contrato v1) | credenciales válidas inician sesión; inválidas y usuario inactivo devuelven el mismo error, sin distinguirse |
| UT-03 | Alta, listado y edición de usuarios | RF-002 | UT-01 | UT-02 | `GET/POST /usuarios`, `PATCH /usuarios/{id}` (contrato v1) | contrato cumplido; un administrador no puede quitarse su propio rol ni desactivarse |
| UT-04 | Control de acceso deny-by-default aplicado como mecanismo transversal, con la matriz de permisos como fuente | RF-002, RNF-013 | UT-01 | UT-05 | middleware y política de autorización por rol | una ruta sin regla declarada rechaza; las pruebas cubren sin autenticar, autenticado con permiso, autenticado sin permiso y credencial inválida |
| UT-05 | Fundación compartida: tablas `auditorias` y `log_errores` creadas, con los privilegios de la aplicación restringidos a insertar y leer sobre auditoría | MIG-009, RNF-004 | UT-01 | UT-04 | schema de `Auditoria` y `LogError` del modelo aprobado | la migración aplica desde base limpia; un intento de actualizar o borrar una fila de auditoría desde la aplicación falla por privilegios, no solo por convención |
| UT-06 | Livewire instalado y operativo sobre el layout base que dejó S-00, con un componente de humo que renderiza y responde a una interacción | ADR-0005, docs/frontend/integracion.md, docs/contratos/servicios-de-dominio.md | ninguna | UT-01, UT-02, UT-03, UT-04, UT-05 | `composer.json` declara `livewire/livewire`; el componente vive bajo `app/Dominios/<Dominio>/Livewire/`, según la convención ya declarada en `AGENTS.md` | `composer show livewire/livewire` devuelve la versión instalada; el componente de humo renderiza dentro del layout base y una interacción actualiza el DOM sin recarga completa; las tres suites y `pnpm build` siguen en verde |
