## RFC — S-00 · Fundación técnica
- RF que implementa: ninguno directamente. Es la base sobre la que todos los demás se construyen.
- Rol: implementation (backend)
- Alcance de este sprint: dejar el proyecto Laravel creado, corriendo en local contra PostgreSQL, con Tailwind compilando por Vite, la estructura `app/Dominios/` establecida, y los comandos de lint, pruebas y build funcionando de verdad — no prometidos. **Explícitamente no**: ninguna entidad de negocio, ninguna migración de dominio, ninguna pantalla funcional.
- Documentos relacionados: ADR-0001 (PostgreSQL), ADR-0005 (repositorio único), RNF-006 (fecha, hora y moneda), AGENTS.md (comandos de verificación declarados como `previsto en S-00`)
- Contratos que toca: ninguno
- Estado: propuesto
- Aprobado por: pendiente — fecha: pendiente

## Unidades de trabajo
| ID | Resultado observable | Deriva de | Depende de | Paralelizable con | Interfaz fijada | Criterio de cierre |
|---|---|---|---|---|---|---|
| UT-01 | Proyecto Laravel creado con la versión estable vigente, verificada contra la fuente oficial, corriendo sobre PHP 8.5 | ADR-0005, AGENTS.md | ninguna | ninguna | versión de Laravel y de PHP registradas en `AGENTS.md` y en el README | `php artisan serve` levanta y responde; la versión queda registrada como verificada, no como recordada |
| UT-02 | Conexión a PostgreSQL configurada, con zona horaria UTC en la conexión y precisión decimal exacta para dinero | ADR-0001, RNF-006 | UT-01 | UT-03 | `.env.example` ya escrito; `DB_CONNECTION=pgsql` | una migración de prueba aplica y revierte desde base limpia; una prueba confirma que un instante guardado se lee como el mismo momento UTC |
| UT-03 | Assets con Vite y Tailwind, y layout base vacío que compila | AGENTS.md (build) | UT-01 | UT-02 | `pnpm build` | el build produce los assets y el layout base renderiza sin errores |
| UT-04 | Estructura `app/Dominios/` establecida con el autoload funcionando, y una prueba de arquitectura que falla si alguien crea código de dominio fuera de ella | ADR-0005, laravel-estructura | UT-01 | ninguna | namespace `App\Dominios\<Dominio>` | la prueba de arquitectura detecta y rechaza una clase de dominio ubicada en `app/Models` |
| UT-05 | Comandos de verificación reales: Pint para lint, PHPUnit separado en Unit y Feature, y ambos corriendo en verde sobre el proyecto vacío | AGENTS.md | UT-01 | UT-02, UT-03 | `./vendor/bin/pint --test`, `php artisan test --testsuite=Unit`, `php artisan test --testsuite=Feature` | los tres comandos corren y pasan; sus salidas reemplazan los `previsto en S-00` de `AGENTS.md` |
