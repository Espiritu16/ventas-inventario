## RFC — S-04-B · Inventario por lotes, compras y kardex
- RF que implementa: RF-006, RF-007, RF-008, RF-009
- Rol: implementation (backend)
- Alcance de este sprint: el corazón del inventario. La compra crea lotes con vencimiento y costo, el stock se consulta por lote, cada movimiento queda en un kardex inmutable y el ajuste manual exige motivo. **Explícitamente no**: la salida por venta, que es S-05-B; ni la alerta de vencimiento, que es S-07-B.
- Documentos relacionados: ADR-0004, MIG-004, MIG-005, RNF-003, RNF-004, docs/persistencia/modelo.md
- Contratos que toca: docs/contratos/compras.md (v1), docs/contratos/inventario.md (v1)
- Estado: propuesto
- Aprobado por: pendiente — fecha: pendiente

## Unidades de trabajo
| ID | Resultado observable | Deriva de | Depende de | Paralelizable con | Interfaz fijada | Criterio de cierre |
|---|---|---|---|---|---|---|
| UT-01 | Fundación compartida: entidades `Lote` y `MovimientoInventario` con sus migraciones, la restricción que impide cantidad negativa, y los privilegios que impiden actualizar o borrar el kardex | ADR-0004, MIG-004, MIG-005, RNF-004 | ninguna | ninguna | schema de `lotes` y `movimientos_inventario` | dejar un lote en negativo falla por restricción de la base; actualizar un movimiento falla por privilegios |
| UT-02 | Servicio de inventario: única puerta de entrada para ingresar, descontar y ajustar existencias, siempre dentro de una transacción y escribiendo el kardex | RF-006, RF-008, RNF-003 | UT-01 | ninguna | `InventarioService`: `ingresar()`, `descontarPorVencimiento()`, `ajustar()` | ninguna otra clase escribe en `lotes`; una prueba de arquitectura lo verifica. `descontarPorVencimiento()` queda disponible para S-05-B |
| UT-03 | Registro de compra que crea o acumula lotes y genera sus movimientos, todo atómico | RF-006 | UT-02 | UT-04 | `POST /compras`, `GET /compras`, `GET /compras/{id}` | una compra con vencimiento pasado se rechaza; un documento repetido del mismo proveedor se rechaza; si algo falla no queda ni compra ni stock |
| UT-04 | Consulta de stock por producto y lote, ordenada por vencimiento, y consulta de kardex | RF-007, RF-008 | UT-02 | UT-03 | `GET /inventario`, `GET /inventario/kardex` | el stock disponible excluye lotes vencidos; el vendedor no recibe el costo; la suma de movimientos de un lote coincide con su cantidad actual |
| UT-05 | Ajuste manual con motivo obligatorio y bloqueo de la fila del lote | RF-009 | UT-02 | ninguna | `POST /inventario/ajustes` | sin motivo se rechaza; un ajuste que dejaría negativo se rechaza; el ajuste queda en el kardex con su responsable y en auditoría |
